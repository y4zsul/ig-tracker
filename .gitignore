# Scratch build output. The shareable zip lives in download/ and IS tracked,
# so non-developers have something to click that isn't a git checkout.
dist/

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
*.swp
